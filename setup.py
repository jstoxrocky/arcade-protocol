from setuptools import setup, find_packages

setup(
    name='contract_tester',
    version="0.1",
    description="Contract testing for 0x2048",
    include_package_data=True,
    packages=find_packages(),
)
